RayTracer

creator: Taylor Dall

how to use: Provide a json file with scene data. Run from the command line.

usage: java -jar RayTracer.jar <json_filename> <width> <height> <format>

supported formats: png (default), jpeg, gif, ppm, bpm, wbpm